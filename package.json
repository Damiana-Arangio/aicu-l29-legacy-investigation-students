{
  "name": "aicu-l29-legacy-investigation-starter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <27"
  },
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "reset": "node scripts/reset-data.js",
    "check": "node --check server/index.js && node --check server/app.js && node --check server/ticket-repository.js && node --check server/ticket-service.js && node --check src/main.js && node --check scripts/reset-data.js",
    "test": "node --test \"tests/api/*.test.js\"",
    "verify": "pnpm check && pnpm test"
  }
}
